# Handoff BGMG Chile — 2026-05-28

> Snapshot del estado del proyecto al cerrar la sesión del 27/28 de mayo. Continúa el [HANDOFF-2026-05-27.md](./HANDOFF-2026-05-27.md). Todas las versiones, archivos y decisiones están verificadas.

---

## 1. Estado de los plugins (verificado)

| Pieza | Versión código | ZIP | Ubicación local |
|---|---|---|---|
| `bgmg-tema-base` | **1.1.0** | ✓ `zips/bgmg-tema-base.zip` | `themes/bgmg-tema-base/` |
| `bgmg-chile` | **1.18.0** | ✓ `zips/bgmg-chile.zip` (127 KB) | `plugins/bgmg-chile/` |
| `bgmg-landing` | **6.4.6** | ✓ `zips/bgmg-landing.zip` (99 KB) | `plugins/bgmg-landing/` |
| `beautygirlmg-mayorista` | **2.5.2** | ✓ `zips/beautygirlmg-mayorista.zip` (79 KB) | `plugins/beautygirlmg-mayorista/` |

### Consistencia verificada
- `bgmg-chile.php` header `Version: 1.18.0` ✓ + `BGMG_CHILE_VERSION = '1.18.0'` ✓ + `readme.txt` Stable tag `1.18.0` ✓
- `bgmg-landing.php` header `Version: 6.4.6` ✓
- `beautygirlmg-mayorista.php` header `Version: 2.5.2` ✓ + `BGM_VERSION = '2.5.2'` ✓
- `bgmg-tema-base/style.css` `Version: 1.1.0` ✓

---

## 2. Sitios involucrados

Sin cambios respecto del handoff anterior:

| Dominio | Rol |
|---|---|
| `beautygirlmg.cl` | V1 producción actual. 1.325 productos. Sin nuestros plugins. |
| `new.beautygirlmg.cl` | V2 destino de los plugins. Verificada en vivo en esta sesión. |

**Decisión arquitectónica reconfirmada:** la migración V1 → V2 será **manual y limpia**. Solo se trasladan precios, productos y métodos de pago. NO se copian plugins ni temas del V1. Esto neutraliza el bug histórico `wp is not defined` (43% sesiones V1) que NO viajará al V2.

---

## 3. Cambios ejecutados en esta sesión

### 3.1. Tema base
| Versión | Cambio |
|---|---|
| 1.0.0 → **1.1.0** | Agregado `add_theme_support( 'custom-logo', [...] )` en `functions.php`. Habilita el control "Logo" en wp-admin → Apariencia → Personalizar → Identidad del sitio. El código del header de bgmg-landing ya leía `get_theme_mod('custom_logo')`, solo faltaba registrar el support. |

### 3.2. `bgmg-chile`
| Versión | Cambio |
|---|---|
| 1.15.2 → 1.16.0 | (a) Eliminado bloque "Datos para boleta/factura" de emails al cliente y de "Mi cuenta → Detalle del pedido" (y por extensión thank-you). El RUT sigue mostrándose dentro del bloque de dirección de facturación vía `woocommerce_order_formatted_billing_address`. Admin → Pedidos sigue mostrando el RUT intacto. (b) Código postal oculto vía CSS en checkout y formularios de dirección (campo sigue en DOM por si una pasarela futura lo requiere). |
| 1.16.0 → 1.17.0 | **Fase 10 hardening (sin caché):** D1 fallback de menú padre — los 3 wizards usan helper `bgmg_chile_wizard_register_submenu()` que cae a top-level si `bgmg-despachos` no existe. D2 validación WhatsApp en editor inline del wizard de envíos (usa `BGMG_Chile_Telefono_Validator::is_valid_movil` + normaliza al formato canónico). D3 pre-flight check (WooCommerce activo + `WC_Shipping_Zones` disponible) en los 3 wizards, con mensaje admin amigable si falta algo. **F3 (en bgmg-landing)** — ver tabla siguiente. **Caché de stats SKIPPED intencionalmente** — se aplicará post-deploy con datos reales. |
| 1.17.0 → **1.18.0** | **Fase 11 — Reportes de despachos.** Reemplaza el placeholder "En construcción" del submenú Despachos BGMG → Reportes con pantalla real. 3 secciones: (1) Resumen del período (total pedidos, ingresos por envío, promedio). (2) Ranking de comunas top 10 con check de si tienen tarifa configurada. (3) Métodos de envío con ratio + ingresos. Selector de ventana 7/30/90 días vía `?dias=X`. Solo cuenta `processing` + `completed`. Sin caché en esta versión. |

### 3.3. `bgmg-landing`
| Versión | Cambio |
|---|---|
| 6.3.7 → 6.4.0 | Mi cuenta → Direcciones consolidado a UN cuadro "Mi dirección / Envío" que edita billing (donde vive el RUT). El cuadro de "Facturación" separado fue removido. Detalle de pedido también muestra una sola dirección. |
| 6.4.0 → 6.4.1 | **Bug fix:** botón "Editar" no abría nada. Causa: WC traduce el slug del subendpoint a `facturacion`/`envio` según idioma, pero el dispatcher comparaba contra `billing`/`shipping` en inglés. Fix con `wc_edit_address_i18n($x, true)` que normaliza el slug localizado al estándar antes de despachar. |
| 6.4.1 → 6.4.2 | Estilos del form de edit-address: selects (Región/Comuna) con chevron SVG custom + fondo rosa, `input[type="tel"]` con mismo estilo que text/email, botón "Guardar dirección" con estilo bgmg consistente. |
| 6.4.2 → 6.4.3 | Rebrand frontend: "Dirección de facturación" → "Dirección de envío". Implementación: filtro `gettext` que mapea 4 strings, solo en frontend (admin queda intacto). Mantenidos intactos los strings relacionados a "factura" como documento tributario. |
| 6.4.3 → 6.4.4 | **F3 del HANDOFF anterior:** Breadcrumb eliminado del checkout principal. Solo queda el botón "← Volver al carrito". El breadcrumb se mantiene en thank-you (`is_order_received_page()`) con `aria-current="page"`. |
| 6.4.4 → 6.4.5 | En detalle de pedido (Mi cuenta → ver pedido) pasamos `mostrar_factura => false` al helper `bgmg_chile_render_order_summary()`. Quita el bloque RUT de esa vista (decisión coherente con el cambio de 1.16.0 en bgmg-chile). |
| 6.4.5 → **6.4.6** | **Bug fix encontrado en testeo:** Short description aparecía DUPLICADO en página de producto. Causa: `remove_action('woocommerce_single_product_summary', 'woocommerce_template_single_excerpt', 25)` — la prioridad estaba mal, WC registra el hook en prio 20, no 25. Cambiado a 20 → el hook nativo de WC ya se remueve correctamente y solo aparece el render manual del template. |

### 3.4. `beautygirlmg-mayorista`
| Versión | Cambio |
|---|---|
| 2.5.1 → **2.5.2** | **Auditoría y eliminación de código muerto.** Removido el módulo de migración v1→v2 entero: archivo `includes/core/migracion.php` borrado, `require_once` eliminado del bootstrap, llamada `bgm_migrar_v1_a_v2()` eliminada del `activation_hook`, 4 opciones huérfanas (`bgm_migracion_v1_*`) eliminadas del uninstall, README.md y CLAUDE.md actualizados. Motivación: V2 es instalación limpia, nunca tuvo el plugin v1 viejo, la migración automática NO aplica al caso. **Bonus:** el módulo tenía un bug (línea 94 era texto fuera de PHP) que en V2 nunca se disparaba porque `$count = 0` siempre. |

---

## 4. Datos reales validados en vivo (2026-05-27)

### 4.1. Compra de prueba completa BACS
- Pedido **#1132** creado end-to-end con éxito
- Cliente: "Jose Garcia", RUT `27.369.314-9`, dirección Av. Ejército Libertador 634, dpto 1202, Santiago RM
- Método: Transferencia bancaria directa
- Total: $6.990 (Bálsamo de Frutilla $3.490 + tarifa fija Santiago $3.500)
- Email "en espera" llegó con asunto custom "Recibo del pedido #1132"
- Mensaje thank-you custom mostrado correctamente
- RUT validado y guardado en `_bgmg_rut`
- Notas del pedido fluyen al email

### 4.2. Configuraciones verificadas
- HPOS activado ✓
- 3 plugins activos en última versión ✓
- Menú "Despachos BGMG" presente con 7 submenús: Resumen, Tarifas RM, Retiro en tienda, Reportes, Asistente Envíos, Asistente Checkout, Operativa diaria
- Wizards Asistente Envíos / Checkout / Operativa abren sin error
- Reportes era el único placeholder real (ya reemplazado en 1.18.0)
- Cascada Región → Comuna en checkout funcionando
- Starken + Bluexpress aparecen separados como "Por pagar"

### 4.3. Detalles que NO son bugs
- Slug oficial del checkout en este sitio es `/finalizar-compra/` (español), no `/checkout/`. URL `/checkout/` da 404 — comportamiento esperado, no requiere acción.
- Cuando una comuna RM tiene tarifa fija configurada (Santiago), esa tarifa REEMPLAZA las opciones "Por pagar". Es comportamiento esperado.

---

## 5. Decisiones de producto / arquitectónicas tomadas

1. **Bloque "Datos para boleta/factura"**: removido de la vista del cliente final (email, thank-you, Mi cuenta → detalle del pedido). Mantenido en admin → Pedidos. RUT sigue saliendo dentro del bloque de dirección de facturación.
2. **Mi cuenta → Direcciones**: un solo cuadro "Mi dirección / Envío" que edita billing (donde vive el RUT). No mostramos billing y shipping separados.
3. **Rebrand frontend**: cliente ve "Dirección de envío" en todos lados. Admin mantiene los términos nativos de WC (Facturación / Envío).
4. **Toggle "Necesito factura (empresa)"** en checkout: mantiene la palabra "factura" — refiere al documento tributario, no a la dirección.
5. **Código postal**: oculto vía CSS, no eliminado del DOM. Reversible si una pasarela futura lo requiere.
6. **Breadcrumb en checkout**: removido. Solo botón "Volver al carrito". Se mantiene en thank-you.
7. **Migración V1 → V2**: manual y limpia. Plan del usuario es crear un script aparte que tome datos exportados del V1 y los transforme al formato del V2 antes de importar. NO se usará la migración automática del plugin (que solo aplicaba al viejo v1 del mayorista).
8. **Cache de stats wizards / reportes**: NO se implementó. Se configurará post-deploy con LiteSpeed Cache + datos reales si llega a ser problema.
9. **Tema técnico**: tests con estado (carrito, login, compra) los hace el usuario manualmente; Claude solo verifica URLs públicas sin sesión.
10. **Meta declarada por el usuario**: 50K visits/mes (V1 actualmente tiene ~33K).

---

## 6. Pendientes priorizados

### 🔴 Urgente (configuración wp-admin, sin código)
- [ ] **Activar "Crear cuenta en checkout"**: WC → Ajustes → Cuentas y privacidad → ☑ "Permitir crear cuenta durante el checkout" + ☑ "Permitir login durante el checkout".
- [ ] **Cambiar título del sitio**: WP → Ajustes → General → cambiar "BeautyNew3" → "BeautyGirlMG". Hoy los emails salen con el nombre técnico de staging.

### 🟡 Próxima sesión (probable)
- [ ] **Script de migración manual V1 → V2.** El usuario lo propuso. Necesita pensarse en conjunto:
  - ¿Cómo va a exportar el V1? (CSV de WC nativo, plugin de export, dump SQL?)
  - ¿Qué datos trasladar? (precios regulares, stock, mayoristas, imágenes, categorías, descripciones, atributos?)
  - ¿V1 tiene algún plugin de mayorista actualmente? Si sí, ¿cuál y cómo guarda los datos?
  - ¿Salida del script: CSV listo para importar con WP All Import? ¿JSON? ¿SQL?
- [ ] **LiteSpeed Cache presets para WC** (cuando esté listo el deploy a producción). Documento corto con qué cachear, qué excluir (carrito, checkout, mi cuenta, ajax) y cómo invalidar tras un deploy.

### 🟢 Post-deploy (cuando haya tráfico real)
- [ ] **Caché de stats wizards + Reportes**: transient de 5 min. Implementar si Query Monitor o New Relic muestran > 50ms en `wc_get_orders` con limit=-1.
- [ ] **Refactor CSS inline**: extraer los `<style>` de los templates de bgmg-landing (account, checkout, shop, product, cart, category) a archivos .css separados con `wp_enqueue_style`. Beneficio real solo a partir de 30K+ visits. Ver memoria `project_bgmg_tech_debt.md`.

### ⚪ Opcional / baja prioridad
- Auditoría profunda de `inc/wizard/wizard-checkout.php` y `wizard-operativa.php` (ya validados en vivo, pero no leídos línea por línea por Claude en esta sesión).
- Revisar el archivo `BANNERS-CUSTOMIZER-PLAN.md` de bgmg-landing — su línea 68 sigue diciendo info incorrecta sobre el logo ("no requiere trabajo adicional") cuando en realidad había que registrar `add_theme_support('custom-logo')`. Es solo doc desactualizada, no rompe nada.
- Sección 11 de `beautygirlmg-mayorista/SPECS.md` describe la migración v1→v2 que ya eliminamos. Doc histórica, no afecta funcionamiento.

---

## 7. Cómo continuar en otra sesión

1. Leer este archivo (`HANDOFF-2026-05-28.md`).
2. Verificar versiones contra lo declarado en sección 1:
   - `grep "Version:" plugins/bgmg-chile/bgmg-chile.php`
   - `grep "Version:" plugins/bgmg-landing/bgmg-landing.php`
   - `grep "Version:" plugins/beautygirlmg-mayorista/beautygirlmg-mayorista.php`
   - `grep "Version:" themes/bgmg-tema-base/style.css`
3. Releer la memoria persistente:
   - `~/.claude/projects/.../memory/project_bgmg_v2_deploy_status.md` — estado del deploy
   - `~/.claude/projects/.../memory/project_bgmg_tech_debt.md` — mejoras anotadas no urgentes
   - `~/.claude/projects/.../memory/feedback_bgmg_testing.md` — los tests con estado los hace el usuario
   - `~/.claude/projects/.../memory/user_origen.md` — el usuario es venezolano, hablar con "tú"
4. Sugerir continuar por **Script de migración V1 → V2** (es lo que el usuario pidió explícitamente para próxima sesión).

### Comando para regenerar TODOS los ZIPs (PowerShell, si hace falta)

```powershell
function New-PluginZip {
  param([string]$SourceDir, [string]$EntryPrefix, [string]$ZipPath)
  Add-Type -AssemblyName System.IO.Compression.FileSystem | Out-Null
  if (Test-Path $ZipPath) { Remove-Item $ZipPath -Force }
  $zip = [System.IO.Compression.ZipFile]::Open($ZipPath, 'Create')
  try {
    $base = (Resolve-Path $SourceDir).Path.TrimEnd('\','/')
    Get-ChildItem -Path $base -Recurse -File | ForEach-Object {
      $rel = $_.FullName.Substring($base.Length).TrimStart('\','/').Replace('\','/')
      [void][System.IO.Compression.ZipFileExtensions]::CreateEntryFromFile($zip, $_.FullName, "$EntryPrefix/$rel", [System.IO.Compression.CompressionLevel]::Optimal)
    }
  } finally { $zip.Dispose() }
}
$root = 'G:\Otros ordenadores\Mi portátil\Google drive proyectos\bgmg-chile-proyecto'
New-PluginZip "$root\plugins\bgmg-chile"             "bgmg-chile"             "$root\zips\bgmg-chile.zip"
New-PluginZip "$root\plugins\bgmg-landing"           "bgmg-landing"           "$root\zips\bgmg-landing.zip"
New-PluginZip "$root\plugins\beautygirlmg-mayorista" "beautygirlmg-mayorista" "$root\zips\beautygirlmg-mayorista.zip"
New-PluginZip "$root\themes\bgmg-tema-base"          "bgmg-tema-base"         "$root\zips\bgmg-tema-base.zip"
```

**Importante: NO usar `Compress-Archive` directamente** — genera ZIPs con backslashes que rompen instalación en WordPress.

---

## 8. Checklist post-deploy (cuando suba la última tanda)

- [ ] Subir los 4 zips: tema + 3 plugins desde wp-admin (Plugins/Apariencia → Subir → "Reemplazar")
- [ ] **wp-admin → Ajustes → Enlaces permanentes → Guardar cambios** (flushea rewrite rules; necesario después de cada update de bgmg-landing)
- [ ] Ir a Despachos BGMG → **Reportes**: confirmar que se ven las 3 secciones sin error
- [ ] Página de producto Botox Capilar: confirmar que el short description aparece UNA sola vez
- [ ] Mi cuenta → Direcciones → Editar: confirmar que el form abre con los estilos completos
- [ ] Hacer un pedido de prueba: confirmar que no aparece "Datos para boleta/factura" en thank-you ni email
- [ ] Wizard Envíos → editor de retiro: poner WhatsApp `asdf` → debe rechazar con mensaje claro

---

## 9. Changelog resumido

```
bgmg-tema-base: 1.0.0 → 1.1.0  (custom-logo support)

bgmg-chile: 1.15.2 → 1.18.0
  1.16.0  remover bloque RUT del email/thank-you + esconder código postal
  1.17.0  Fase 10 hardening (D1+D2+D3, sin caché)
  1.18.0  Fase 11 Reportes de despachos

bgmg-landing: 6.3.7 → 6.4.6
  6.4.0  consolidación direcciones (un cuadro)
  6.4.1  fix wc_edit_address_i18n (slug español no matcheaba)
  6.4.2  estilos completos al form de edit-address
  6.4.3  rebrand "facturación" → "envío" en frontend
  6.4.4  breadcrumb removido del checkout
  6.4.5  mostrar_factura=false en detalle pedido
  6.4.6  fix prioridad remove_action excerpt (era 25, debía ser 20)

beautygirlmg-mayorista: 2.5.1 → 2.5.2
  - removido módulo de migración v1→v2 (código muerto en este caso)
```

---

*Snapshot generado al cerrar la sesión del 27-28 de mayo de 2026.*
